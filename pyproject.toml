[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "braillebaseviet"
version = "0.2.8"
description = "A complete and extensible Unicode Braille processing library. Viet language version"
authors = [
    { name = "Nagao Yuji" }
]
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.6"

dependencies = [
    "braillebase>=0.2.7"
]

keywords = ["braille", "unicode", "accessibility", "API", "binary", "blind", "education"]

[project.urls]
Homepage = "https://github.com/DukaCrazy/braillebaseviet"
Documentation = "https://github.com/DukaCrazy/braillebaseviet"
Source = "https://github.com/DukaCrazy/braillebaseviet"
